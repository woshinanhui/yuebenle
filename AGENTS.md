# 阅本乐知识库工作规则

本目录是剑桥英语培训机构的内部知识库，主要用于检索资料、生产内容和制定方案。

忽略 `_系统/旧版组件_可删除`，其中内容已停用，不得作为知识来源或运行工具。

## 默认工作方式

- 用户说“整理入库”或含义相近的话时，检查 `00_待入库` 中的 `.md` 和 `.docx` 文件并完成入库。
- 入库属于用户明确授权的本地整理操作；可以读取、移动和创建本知识库内的文件，但不得上传资料、对外发送或调用外部服务。
- 原文件先复制到 `01_原始资料` 的适当分类中，保持原内容不变；待入库区的文件保留至用户确认后再清理。
- Word 文件在 `02_忠实转写` 生成对应 Markdown；Markdown 原件也在该层生成规范化副本。忠实转写不得摘要、删减或改写事实。
- 在 `03_知识库` 生成便于检索和复用的知识文件，并更新 `05_知识地图/知识库总览.md` 及相应主题地图。
- 找不到可靠分类时保留在 `00_待入库/03_待判断`，不要猜测机构事实。

## 来源优先级

1. 现行正式制度、机构确认事实
2. 官方教材、教师用书和考试官方资料
3. 机构正式课程与教研资料
4. 内部案例和经验总结
5. AI 整理内容

冲突时采用更高优先级、更新版本；无法判断时并列说明，不自行裁决。

## 教材资料处理

- 识别并保留：考试体系、教材系列、级别、版本、资料角色。
- 资料角色优先使用：学生用书、教师用书、练习册、音频文本、测评资料、配套资源。
- 教材按“系列 → 级别 → 版本 → 资料角色”组织。
- 不把整本教材机械拆成大量碎片。默认生成教材总览和单元级笔记；只有独立、可复用的教学方法或语言点才单独成卡。
- 检索教材时先读 `03_知识库/02_教材体系/教材使用优先级.md`；默认优先使用 `02_AI整理稿` 或 `02_结构化整理稿`，核对原文时再读 `01_原始OCR` 和 `01_原始资料`。
- 普通内容生产不得默认检索 `03_测试题转写` 中的答案文件；仅在测评相关任务中按 `answer_status` 选择题目或答案。

## 模型提醒

- 批量复制、清单核对、忠实转写、规范命名和链接检查开始前，提醒用户可切换到 `GPT-5.6 Luna（low）`。
- 遇到教材版本辨识、课程进阶判断、跨资料冲突或需要产出方案时，提醒用户切回 `GPT-5.6 Terra（medium）`。
- 模型切换由用户在 Codex 界面完成；不得假称可在同一执行过程中自动切换。

## 运营资料处理

- 优先判断所属职能：招生与市场、课程产品、教学运营、学员服务、教师管理、校区管理、人力资源、财务制度、品牌宣传、SOP与表单。
- 制度和流程必须保留适用范围、生效时间、负责人、版本状态和例外条件。
- 历史、废止和无法确认现行状态的资料移至 `90_历史归档` 或标记 `status: 待确认`，不得当作现行规则引用。

## Markdown 元数据

按资料实际情况填写，不知道的字段写 `待确认`，不要虚构：

```yaml
---
title: ""
domain: 教材与教研 | 校区运营 | 机构事实
document_type: ""
series: ""
level: ""
edition: ""
document_role: ""
department: ""
status: current | draft | archived | 待确认
authority: 官方资料 | 机构正式文件 | 内部经验 | AI整理
effective_date: ""
confidentiality: public | internal | sensitive
source: "[[来源文件]]"
---
```

只保留适用字段，避免空字段和标签堆砌。

## 内容和方案生产

- 先阅读 `05_知识地图/知识库总览.md`，再定位原始依据。
- 机构自身信息以 `03_知识库/01_机构事实` 为准。
- 教材内容必须注明系列、级别和版本；政策、价格、课程安排必须核对状态和生效日期。
- 结论要链接到来源；AI 知识文件不能替代原件作为最终证据。
- 资料不足时列出缺失信息并向用户确认，不得补造课程优势、师资数据、成绩、校区数量或客户案例。
- 成品保存到 `04_内容生产` 对应目录；未经用户要求，不覆盖已有成品。

## 隐私

- 学员、家长、教师的姓名、电话、成绩、人事、财务和合同信息按敏感资料处理。
- 不在知识文件中复制不必要的个人信息；可用角色或匿名编号替代。
- 不通过外部 API 处理本知识库资料，除非用户针对具体资料明确授权。
